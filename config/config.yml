app:
  name: 'go-service-template'
  version: '1.0.0'

http:
  port: '8080'

logger:
  zap_level: 'debug'
  rollbar_env: 'go-service-template'

postgres:
  pool_max: 2

rabbitmq:
  rpc_server_exchange: 'rpc_server'
  rpc_client_exchange: 'rpc_client'
